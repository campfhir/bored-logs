# Hoist all public packages to the workspace root node_modules so that
# webpack can resolve peer dependencies (luxon, kysely, winston, …) when
# transpiling files inside src/ via transpilePackages.
public-hoist-pattern[]=*

# Don't publish lockfile, local config, or source
ignore-scripts=false
